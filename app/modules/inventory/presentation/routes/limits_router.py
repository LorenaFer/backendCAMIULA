"""Rutas FastAPI para Límites y Excepciones de Despacho.

Pendiente de implementación completa en Fase 5.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/dispatch-limits", tags=["Inventory — Dispatch Limits"])
