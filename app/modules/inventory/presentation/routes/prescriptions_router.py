"""Rutas FastAPI para el recurso Receta Médica (Prescription).

Pendiente de implementación completa en Fase 4.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/prescriptions", tags=["Inventory — Prescriptions"])
