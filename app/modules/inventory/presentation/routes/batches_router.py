"""Rutas FastAPI para el recurso Lote (Batch).

Pendiente de implementación completa en Fase 3.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/batches", tags=["Inventory — Batches"])
